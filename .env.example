# Existing Shopify deployments should set COMMERCE_PROVIDER=shopify to keep current behavior.
COMPANY_NAME="Vercel Inc."
SITE_NAME="Next.js Commerce"
COMMERCE_PROVIDER="mock" # Options: mock (default) or shopify

# Shopify provider configuration (required only when COMMERCE_PROVIDER=shopify)
SHOPIFY_STORE_DOMAIN="[your-shopify-store-subdomain].myshopify.com" # Required for Shopify provider
SHOPIFY_STOREFRONT_ACCESS_TOKEN="" # Required for Shopify provider
SHOPIFY_REVALIDATION_SECRET="" # Optional webhook validation secret for Shopify provider

# NextAuth Configuration
AUTH_SECRET="" # Generate with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000" # Your app URL

# Google OAuth Configuration
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
